import ClientWrapper from "./client-wrapper";

// Disable static rendering and force dynamic rendering
export const dynamic = "force-dynamic";

export default function TokensPage() {
  return <ClientWrapper />;
}
